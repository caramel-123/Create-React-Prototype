
  # Create React Prototype

  This is a code bundle for Create React Prototype. The original project is available at https://www.figma.com/design/h8h2q6ZF3IFq4YjhRjqooc/Create-React-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  